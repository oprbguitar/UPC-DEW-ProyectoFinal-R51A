
<%= form_for(@concert) do |f| %>
  <% if @concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
      <% @concert.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :local %><br />
    <%= f.collection_select :local_id,
    Local.all, :id, :nombre, :prompt => true %>
  </div>
  
  <div class="field">
    <%= f.label :band %><br />
    <%= f.collection_select :band_id,
    Band.all, :id, :nombre, :prompt => true %>
  </div>

  <div class="field">
    <%= f.label :hora_ini %><br />
    <%= f.date_select :hora_ini %>
  </div>
  <div class="field">
    <%= f.label :hora_fin %><br />
    <%= f.date_select :hora_fin %>
  </div>

  <%= button_to "Grabar", :class => "ui-button ui-widget ui-state-default", :method => :get , :form => { "data-type" => "json" }  %>
  
<% end %>

